import React from 'react';

const SongsList = ({ songs }) => {
  return (
    <div>
      <div className='first'>
        <h2>Logo</h2>
      </div>
      <div className='first'>

      </div>
      {/* <h2>Songs List</h2>
      <ul>
        {songs.map((song) => (
          <li key={song.id}>{song.title}</li>
        ))}
      </ul> */}
    </div>
  );
};

export default SongsList;
